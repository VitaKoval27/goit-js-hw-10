<!-- home-work-js-10
task-1 - timer
 1 Імпортуємо бібліотеки
 2 захоплюємо всі єлементи
 3 кнопку дективуємо та навішуємо прослухач подій
 4 оголошуємо дві змінні - одну для отримання дати від користувача, другу-?
 5 викликаємо бібліотеку календря, де одним з агрументів є селектор , другим обект з налаштуваннями та методом обекту з параметром вибраною датою, де ми перевіряємо, що вибрана дата не меньше ніж поточна(new Date()),якщо меньша, то підкючаємо бібліотеку повідомлень з помилкою.
 якщо ні, то робимо кнопку старт активною та установюємо дату як отриманну від котистувача та валідовану.
 6 Додаткова функція конвертації мс в кількість днів, годин, хвилин, секунд
 7 Функція колбек обробки події кнопки(запуск відліку) -робимоо кнопку та каленар не ативними.
 запускаємо сетИнтервал функцію, ака приймає два параметри, другий час кроку - 1000 мс та перший функцію колбек де ми:
 -захоплюємо поточний час (new Data())
 -розраховуємо скільки мілесекунд  до вибраного часу =різница між гетерами поточного часу та часу вибраного
 -розголужуємо логіку щоб зупинити таймер коли він добігає нулю(встановюємо всі часові виміри на 0, робимо кнопку та календар активними)
 -порахувавши кількість днів, годин, хвилин та секунд, отримавши результат виклику функціі конвертування та отримавщи масив як результат.
 - можем присвоїти його значення в єлементи розмітки, але додавши 0 при необхідності, щоб було 2 знака завжди
 - додаткова функція додавання нуля за рахунок методу падСтарт(кількість знаків,)
 -->
<!-- task 2
1. Импортуємо бібліотеку повідомлень
2. Захоплюємо форму всю та инпут окремо
3. навішуємо на форму слухач подій.
4. обробляємо подію субміт фукцією
5. попереджаемо оновлення сторінки
6. захоплюємо значення делай та значення вібраною радіокнопки
7. створюємо проміс, з двома параметрами резолв та реджект
8. у промісі створюємо затриму(сеттаймаут), де другий аргумент делай , перший фукнція де ми розглолужуємо результат на успішний(резолв) та не успішний(реджект).
9 обробляємо проміс де параметр-занчення результату  розголуження, спочатку зен та ми повертаємо повідомлення з бібліотеки - успіх, потім катч у випадку реджекта, та файналі де ми очіщюємо форму у будь якому випадку

 -->
